# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'fastFiles' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks"
  use_frameworks!

  # Pods for fastFiles

  pod 'Zip', '~> 0.6'
  pod 'Highlightr'
  pod 'Swifter', '~> 1.3.2'

end

target 'fastFiles documents' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fastFiles documents

  pod 'Zip', '~> 0.6'

end

target 'fastFiles documentsFileProvider' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fastFiles documentsFileProvider

  pod 'Zip', '~> 0.6'

end


